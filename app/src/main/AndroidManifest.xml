<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.tiqiuzhe.touxiangupdate"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 主界面-->
        <activity android:name=".activity.MainActivity"/>
        <!-- FragmentTabHost界面-->
        <activity android:name=".activity.MainTabActivity"/>

        <!-- 头像设置-->
        <activity android:name=".activity.TouXiangActivity"/>
        <!-- 圆环头像设置-->
        <activity android:name=".activity.YuanHuanActivity"/>
        <!-- 轮播图-->
        <activity android:name=".activity.LunBoTuActivity"/>
        <!-- 轮播图banner-->
        <activity android:name=".activity.LunBoTuBannerActivity"/>
        <!-- 引导页-->
        <activity android:name=".activity.GuideActivity"/>
        <!-- 引导页1-->
        <activity android:name=".activity.GuideActivity1"/>
        <!-- 刷新界面-->
        <activity android:name=".activity.ShuaXinActivity"/>
         <!--爆炸界面-->
        <activity android:name=".activity.ExplosionActivity"/>
        <!--京东刷新界面-->
        <activity android:name=".activity.JDShuaXinActivity"/>
        <!--CheckBox界面-->
        <activity android:name=".activity.CheckBoxActivity"/>
        <!--编辑生活界面-->
        <activity android:name=".activity.LifeAddActivity"/>


    </application>

</manifest>
